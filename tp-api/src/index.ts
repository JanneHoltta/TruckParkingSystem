/**
 * This is the main file of the package, it just re-exports all types.
 * Access all enums and errors as well as API schemas and types like this:
 * `* import { enums, errors, schemas, types } from '@truck-parking/tp-api'`
 */

export * as cookies from './cookies';
export * as enums from './enums';
export * as errors from './errors';
export * as headers from './headers';
export * as schemas from './schemas';
export * as types from './types';
